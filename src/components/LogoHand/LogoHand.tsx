import React from 'react';
import classnames from 'classnames/bind';
import styles from './LogoHand.module.scss';

const cx = classnames.bind(styles);

const LogoHand = () => (
  <div className={cx('LogoHand')} />
);

export default LogoHand;
