import React from 'react';

import './ViewerPanelWrapper.css';

const ViewerPanelWrapper = ({ children }) => ( // eslint-disable-line
  <div className="ViewerPanelWrapper">
    {children}
  </div>
);

export default ViewerPanelWrapper;
