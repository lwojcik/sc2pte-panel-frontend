import React from 'react';
import { render } from '@testing-library/react';
import ConfigDataFetchError from './ConfigDataFetchError';

it('renders correctly', () => {
  render(<ConfigDataFetchError />);
});
