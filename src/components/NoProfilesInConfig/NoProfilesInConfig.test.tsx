import React from 'react';
import { render } from '@testing-library/react';
import NoProfilesInConfig from './NoProfilesInConfig';

it('renders correctly', () => {
  render(<NoProfilesInConfig />);
});
